# Interactions

Ce dossier contient les primitives d'action et de navigation du Design System.

- `Button` déclenche exclusivement une action.
- `Link` conduit exclusivement vers une destination.

Les deux composants interdisent la prop `style`. Les icônes de contenu restent
hors de leur API jusqu'à l'implémentation de `CMP-08`.
