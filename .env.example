# AZUREUM does not require an application environment variable yet.
#
# Future variables will be added only when a validated Sprint introduces the
# corresponding integration. Never commit real secrets or a local .env file.
# The Supabase CLI manages its local service values without this file.
